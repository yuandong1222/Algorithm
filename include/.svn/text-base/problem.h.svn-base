#ifndef _ALGORITHM_PROBLEM_H_
#define _ALGORITHM_PROBLEM_H_

#define INPUT_FILE_NAME  "./bin/test.in"
#define OUTPUT_FILE_NAME "./bin/test.out"

#include <istream>
#include <ostream>

using namespace std;

namespace algorithm {
	class Problem {
		public:
			void SetUp(istream& input, ostream& output);
			void Run(istream& input, ostream& output);
			void TearDown(istream& input, ostream& output);
	};
}

#endif
